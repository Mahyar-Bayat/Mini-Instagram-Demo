import 'package:flutter/material.dart';

class plus_page extends StatelessWidget {
  const plus_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepOrangeAccent,
    );
  }
}
